import { Commitment, Connection, PublicKey, Transaction } from '@solana/web3.js';
import { BaseService } from './base';
import { BagsClaimablePosition } from '../types/meteora';
export declare class FeesService extends BaseService {
    constructor(apiKey: string, connection: Connection, commitment?: Commitment);
    getAllClaimablePositions(wallet: PublicKey): Promise<Array<BagsClaimablePosition>>;
    getClaimTransaction(wallet: PublicKey, position: BagsClaimablePosition): Promise<Array<Transaction>>;
}
//# sourceMappingURL=fees.d.ts.map